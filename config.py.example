opsportal = {
    "api": {
        "base_url": "https://example.org",
        "key": "exampleApiKey"
    }
}

registry = {
    "db": {
        "name": "registry",
        "host": "registry.example.org",
        "user": "registry_admin",
        "password": "registry_secret" 
    }   
}
